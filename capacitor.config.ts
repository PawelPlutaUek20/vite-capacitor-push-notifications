import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pawel.vspn',
  appName: 'com.pawel.vspn',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
